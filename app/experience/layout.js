// app/experience/layout.js

export const metadata = {
  title: "Experience | Dan Humpherson",
  description: "Work experience.",
};

export default function ExperienceLayout({ children }) {
  return <>{children}</>;
}
